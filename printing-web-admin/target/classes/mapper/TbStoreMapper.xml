<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiying.printing.web.admin.dao.TbStoreDao">
    <sql id="tbStoreColumns">
        a.id,
        a.name,
        a.address,
        a.imgurl AS imgUrl,
        a.tel,
        a.shopman,
        a.number,
        a.created,
        a.updated,
        a.isdelete AS isDelete
    </sql>

    <select id="selectAll" resultType="TbStore">
        SELECT
        <include refid="tbStoreColumns"/>
        FROM
        tb_store AS a
        WHERE a.isdelete = 'false'
    </select>

    <select id="getById" resultType="TbStore" parameterType="long">
        SELECT
        <include refid="tbStoreColumns"/>
        FROM
        tb_store as a
        WHERE
        a.id = #{id} AND a.isdelete = 'false'
    </select>

</mapper>