<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiying.printing.web.admin.dao.TbUserDao">
    <sql id="tbUserColumns">
        a.id,
        a.username,
        a.password,
        a.phone,
        a.email,
        a.imgurl,
        a.created,
        a.updated,
        a.isdelete
    </sql>

    <select id="selectAll" resultType="TbUser">
        SELECT
        <include refid="tbUserColumns"/>
        FROM
        tb_user AS a
        WHERE a.isdelete = 'false'
    </select>

    <select id="getById" resultType="TbUser" parameterType="long">
        SELECT
        <include refid="tbUserColumns"/>
        FROM
        tb_user as a
        WHERE
        a.id = #{id} AND a.isdelete = 'false'
    </select>

    <insert id="insert">
    INSERT INTO tb_user (
      username,
      password,
      phone,
      email,
      imgurl,
      created,
      updated
    )
    VALUES
      (
        #{username},
        #{password},
        #{phone},
        #{email},
        #{imgurl},
        #{created},
        #{updated}
      )
     </insert>

    <update id="update">
        UPDATE
            tb_user
        SET
        <if test="username != null and username != ''">
            username= #{username},
        </if>
        <if test="password != null and password != ''">
            password = #{password},
        </if>
        <if test="phone != null and phone != ''">
            phone = #{phone},
        </if>
        <if test="email != null and email != ''">
            email = #{email},
        </if>
        <if test="imgurl != null and imgurl != ''">
            imgurl = #{imgurl},
        </if>
            updated = #{updated}
        WHERE
            id = #{id}
    </update>
    <select id="getByEmail" resultType="TbUser">
        SELECT
        <include refid="tbUserColumns"/>
        FROM
        tb_user AS a
        WHERE
        a.email = #{email} AND a.isdelete = 'false'
    </select>

    <update id="delete">
        UPDATE tb_user
        SET
        isdelete=true
        WHERE id = #{id}
    </update>

    <update id="deleteMulti">
        UPDATE tb_user
        SET
        isdelete=true
        WHERE id IN
        <foreach collection="array" open="(" close=")" separator="," item="id">
            #{id}
        </foreach>
    </update>

    <select id="page" resultType="TbUser" parameterType="java.util.Map">
        SELECT
        <include refid="tbUserColumns"/>
        FROM
        tb_user AS a
        <where>
            <if test="pageParams.username != null and pageParams.username != ''">
                AND a.username LIKE CONCAT('%',#{pageParams.username},'%')
            </if>
            <if test="pageParams.email != null and pageParams.email != ''">
                AND a.email LIKE CONCAT ('%',#{pageParams.email},'%')
            </if>
            <if test="pageParams.phone != null and pageParams.phone != ''">
                AND a.phone LIKE CONCAT ('%',#{pageParams.phone},'%')
            </if>
            AND a.isdelete = 'false'
        </where>
        LIMIT #{start},#{length}
    </select>

    <select id="count" resultType="java.lang.Integer">
        SELECT
        COUNT(*)
        FROM
        tb_user AS a
        <where>
            <if test="username != null and username != ''">
                AND a.username LIKE CONCAT('%',#{username},'%')
            </if>
            <if test="email != null and email != ''">
                AND a.email LIKE CONCAT ('%',#{email},'%')
            </if>
            <if test="phone != null and phone != ''">
                AND a.phone LIKE CONCAT ('%',#{phone},'%')
            </if>
            AND
            a.isdelete = 'false'
        </where>
    </select>
</mapper>